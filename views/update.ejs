<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Student</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
         body {
      font-family: 'Arial', sans-serif;
      background-color: #f0f2f5;
    }

    .navbar-brand {
      font-weight: bold;
      font-size: 1.5rem;
      color: #ffdf00 !important;
    }

    .nav-link {
      font-size: 1.1rem;
      color: #ffffff !important;
      position: relative;
      transition: color 0.3s ease-in-out;
    }

    .nav-link:hover {
      color: #ffdf00 !important;
    }

    .nav-link:hover::after {
      content: '';
      position: absolute;
      width: 100%;
      height: 2px;
      background-color: #ffdf00;
      left: 0;
      bottom: -5px;
      transition: width 0.3s;
    }

    .nav-link i {
      margin-right: 8px;
      color: #ffdf00;
    }

    .nav-link:hover i {
      color: #ffffff;
    }

    /* Dropdown on Hover with Animation */
    .nav-item.dropdown:hover .dropdown-menu {
      display: block;
      animation: fadeIn 0.3s ease-in-out;
    }

    .nav-item.dropdown:hover .nav-link {
      color: #ffdf00 !important;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Dropdown Item Hover */
    .dropdown-item:hover {
      background-color: black;
      color: #ffdf00;
    }
    
        .container {
            max-width: 600px;
            margin-top: 50px;
        }
    
        .card {
            border-radius: 12px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
            animation: fadeInUp 1s ease;
        }
    
        .card-header {
            background-color: #343a40;
            color: white;
            font-size: 1.3rem;
            text-align: center;
            padding: 15px;
            border-radius: 12px 12px 0 0;
        }
    
        .form-label {
            font-weight: bold;
        }
    
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            transition: background-color 0.3s, transform 0.2s;
            width: 200px; /* Adjust width as needed */
            display: block; /* Ensure it takes full width available */
            margin: 0 auto; /* Center the button horizontally */
        }
    
        .btn-primary:hover {
            background-color: #0069d9;
            transform: scale(1.05);
        }
    
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    
        .form-control, .form-check-input {
            transition: box-shadow 0.3s ease-in-out;
        }
    
        .form-control:focus, .form-check-input:checked {
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.8);
        }
    
        .form-check-label:hover {
            color: #007bff;
        }
    
        i {
            color: #ffdf00;
        }
    </style>
    
</head>
<body>
   <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><i class="fas fa-user-graduate" style="color: white;"></i> Student Management</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/"><i class="fas fa-home"></i> Home</a></li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="studentsDropdown" role="button">
              <i class="fas fa-user-graduate"></i> Students
            </a>
            <ul class="dropdown-menu" aria-labelledby="studentsDropdown">
              <li>
                <a class="dropdown-item" href="/students" style="color: black; position: relative;">
                  <i class="fas fa-user-plus" style="color: black;"></i> Add Student
                </a>
                <style>
                  a.dropdown-item:hover {
                    color: #ffdf00 !important;
                    background-color: black!important;
                  }
                  a.dropdown-item:hover i {
                    color: #ffdf00 !important;
                  }

                  
                </style>
                
              </li>
              <li>
                <a class="dropdown-item" href="/adminLogin" style="color: black; position: relative;">
                  <i class="fas fa-users" style="color: black;"></i> View Students
                </a>
                <style>
                  a.dropdown-item:hover {
                    color: #ffdf00 !important;
                    background-color: black!important;
                  }
                  a.dropdown-item:hover i {
                    color: #ffdf00 !important;
                  }

                  
                </style>
                
              </li></ul>
          </li>
          <li class="nav-item"><a class="nav-link" href="/contact"><i class="fas fa-envelope"></i> Contact</a></li>
        </ul>
      </div>
    </div>
  </nav>

    <div class="container">
        <div class="card">
            <div class="card-header">
                Update Student Information
            </div>
            <div class="card-body">
                <form method="post" action="/update/<%= student._id %>">
                    <div class="mb-3">
                        <label for="nameInput" class="form-label"><i class="fas fa-user"></i>&nbsp;&nbsp; Name</label>
                        <input type="text" class="form-control" id="nameInput" name="name" value="<%= student.name %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="mobileInput" class="form-label"><i class="fas fa-phone"></i>&nbsp;&nbsp; Mobile</label>
                        <input type="number" class="form-control" id="mobileInput" name="mobile" value="<%= student.mobile %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="addressTextarea" class="form-label"><i class="fas fa-map-marker-alt"></i>&nbsp;&nbsp; Address</label>
                        <textarea class="form-control" id="addressTextarea" rows="3" name="address" required><%= student.address %></textarea>
                    </div>
                    <div class="mb-3">
                        <label for="cityDropdown" class="form-label"><i class="fas fa-city"></i>&nbsp;&nbsp; City</label>
                        <select class="form-select" id="cityDropdown" name="city" required>
                            <option value="Miraj" <%= student.city === 'Miraj' ? 'selected' : '' %>>Miraj</option>
                            <option value="Kolhapur" <%= student.city === 'Kolhapur' ? 'selected' : '' %>>Kolhapur</option>
                            <option value="Sangli" <%= student.city === 'Sangli' ? 'selected' : '' %>>Sangli</option>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label for="hobbies" class="form-label"><i class="fas fa-heart"></i>&nbsp;&nbsp; Hobbies</label><br>
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" value="Reading" id="hobbyReading" name="hobbie[]" <%= student.hobbie.includes('Reading') ? 'checked' : '' %>>
                            <label class="form-check-label" for="hobbyReading">Reading</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" value="Playing Tabala" id="hobbyTabala" name="hobbie[]" <%= student.hobbie.includes('Playing Tabala') ? 'checked' : '' %>>
                            <label class="form-check-label" for="hobbyTabala">Playing Tabala</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" value="Swimming" id="hobbySwimming" name="hobbie[]" <%= student.hobbie.includes('Swimming') ? 'checked' : '' %>>
                            <label class="form-check-label" for="hobbySwimming">Swimming</label>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label for="gender" class="form-label"><i class="fas fa-venus-mars"></i>&nbsp;&nbsp; Gender</label><br>
                        <div class="form-check">
                            <input class="form-check-input" type="radio" name="gender" id="genderMale" value="Male" <%= student.gender === 'Male' ? 'checked' : '' %> required>
                            <label class="form-check-label" for="genderMale">Male</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input" type="radio" name="gender" id="genderFemale" value="Female" <%= student.gender === 'Female' ? 'checked' : '' %> required>
                            <label class="form-check-label" for="genderFemale">Female</label>
                        </div>
                    </div>
                    <input type="submit" class="btn btn-primary mt-3" value="Update Student">
                </form>
            </div>
        </div>
    </div>
    <br><br><br>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
