<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Student</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f2f5;
        }

        .navbar-brand {
            font-weight: bold;
            font-size: 1.5rem;
            color: #ffdf00 !important;
        }

        .nav-link {
            font-size: 1.1rem;
            color: #ffffff !important;
            position: relative;
            transition: color 0.3s ease-in-out;
        }

        .nav-link:hover {
            color: #ffdf00 !important;
        }

        .nav-link:hover::after {
            content: '';
            position: absolute;
            width: 100%;
            height: 2px;
            background-color: #ffdf00;
            left: 0;
            bottom: -5px;
            transition: width 0.3s;
        }

        .nav-link i {
            margin-right: 8px;
            color: #ffdf00;
        }

        .nav-link:hover i {
            color: #ffffff;
        }

        .container {
            max-width: 800px;
            margin-top: 50px;
        }

        .card {
            border-radius: 12px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
            animation: fadeInUp 1s ease;
        }

        .card-header {
            background-color: #343a40;
            color: white;
            font-size: 1.5rem;
            text-align: center;
            padding: 15px;
            border-radius: 12px 12px 0 0;
            animation: slideIn 0.8s ease-out;
        }

        .card-body {
            padding: 20px;
        }

        table {
            width: 100%;
            margin-bottom: 1rem;
            color: #333;
            text-align: center; /* Center-align text in table cells */
        }

        table th, table td {
            padding: 0.75rem;
            vertical-align: middle;
            border-top: 1px solid #dee2e6;
        }

        table th {
            background-color: #f8f9fa;
            font-weight: bold;
        }

        table td {
            background-color: #ffffff;
        }

        .card-body p {
            font-size: 0.9rem;
            color: #333;
        }

        .card-body p strong {
            color: #007bff;
        }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            transition: background-color 0.3s, transform 0.3s;
            position: relative;
        }

        .btn-primary:hover {
            background-color: #0069d9;
            animation: shake 0.5s ease-in-out;
        }

        @keyframes pulse {
            0% {
                box-shadow: 0 0 0 0 rgba(0, 123, 255, 0.4);
            }
            70% {
                box-shadow: 0 0 0 10px rgba(0, 123, 255, 0);
            }
            100% {
                box-shadow: 0 0 0 0 rgba(0, 123, 255, 0);
            }
        }

        @keyframes shake {
            0% { transform: translateX(0); }
            25% { transform: translateX(-10px); }
            50% { transform: translateX(10px); }
            75% { transform: translateX(-10px); }
            100% { transform: translateX(0); }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes bounceIn {
            0% {
                opacity: 0;
                transform: scale(0.5);
            }
            50% {
                opacity: 1;
                transform: scale(1.1);
            }
            100% {
                opacity: 1;
                transform: scale(1);
            }
        }

        i {
            color: #ffdf00;
        }

        .icon {
            color: #007bff;
            margin-right: 10px;
        }

        .student-details-heading {
            font-size: 1.2rem;
            animation: bounceIn 1s ease;
        }

        .text-center-custom {
            text-align: center;
        }

        .d-flex-center {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/"><i style="color: white;" class="fas fa-user-graduate"></i> Student Management</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/"><i class="fas fa-home"></i> Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/students"><i class="fas fa-user-graduate"></i> Students</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/contact"><i class="fas fa-envelope"></i> Contact</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <h1 class="text-center bg-secondary text-light py-3 student-details-heading">Student Details</h1>
        <div class="card mt-4">
            <div class="card-header">
                <h2><%= student.name %> (ID: <%= student.id %>)</h2>
            </div>
            <div class="card-body">
                <table class="table">
                    <tbody>
                        <tr>
                            <th><i class="fas fa-phone icon"></i> Mobile</th>
                            <td><%= student.mobile %></td>
                        </tr>
                        <tr>
                            <th><i class="fas fa-home icon"></i> Address</th>
                            <td><%= student.address %></td>
                        </tr>
                        <tr>
                            <th><i class="fas fa-city icon"></i> City</th>
                            <td><%= student.city %></td>
                        </tr>
                        <tr>
                            <th><i class="fas fa-star icon"></i> Hobbies</th>
                            <td><%= student.hobbie.join(', ') %></td>
                        </tr>
                        <tr>
                            <th><i class="fas fa-genderless icon"></i> Gender</th>
                            <td><%= student.gender %></td>
                        </tr>
                    </tbody>
                </table>
                <div class="d-flex-center">
                    <a href="/viewStudents" class="btn btn-primary">Back to Student List</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
