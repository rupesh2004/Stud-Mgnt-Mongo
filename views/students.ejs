<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Student Information</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f0f2f5;
    }

    .navbar-brand {
      font-weight: bold;
      font-size: 1.5rem;
      color: #ffdf00 !important;
    }

    .nav-link {
      font-size: 1.1rem;
      color: #ffffff !important;
      position: relative;
      transition: color 0.3s ease-in-out;
    }

    .nav-link:hover {
      color: #ffdf00 !important;
    }

    .nav-link:hover::after {
      content: '';
      position: absolute;
      width: 100%;
      height: 2px;
      background-color: #ffdf00;
      left: 0;
      bottom: -5px;
      transition: width 0.3s;
    }

    .nav-link i {
      margin-right: 8px;
      color: #ffdf00;
    }

    .nav-link:hover i {
      color: #ffffff;
    }

    /* Dropdown on Hover with Animation */
    .nav-item.dropdown:hover .dropdown-menu {
      display: block;
      animation: fadeIn 0.3s ease-in-out;
    }

    .nav-item.dropdown:hover .nav-link {
      color: #ffdf00 !important;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Dropdown Item Hover */
    .dropdown-item:hover {
      background-color: black;
      color: #ffdf00;
    }


    .form-label {
      font-weight: bold;
      color: #333;
      position: relative;
      padding-left: 30px;
    }

    .form-label i {
      position: absolute;
      left: 0;
      top: 50%;
      transform: translateY(-50%);
      color: #ffdf00;
    }

    .form-control, .form-select {
      border-radius: 0.5rem;
      padding-left: 40px;
      transition: box-shadow 0.3s ease-in-out;
    }

    .form-control:focus, .form-select:focus {
      box-shadow: 0px 0px 10px rgba(255, 223, 0, 0.5);
    }

    .btn-primary {

      border: none;
      font-weight: bold;
      transition: background-color 0.3s ease-in-out, transform 0.3s ease-in-out;
    }

    .btn-primary:hover {

      transform: translateY(-3px);
      box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.1);
    }

    .container-fluid {
      margin-top: 30px;
      animation: fadeIn 1s ease-in-out;
    }

    .card {
      border-radius: 0.5rem;
      box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
    }

    .card:hover {
      transform: scale(1.02);
      box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2);
    }

    .view-students-btn {
      background-color: #28a745;
      color: #fff;
      font-weight: bold;
      margin-top: 20px;
      transition: background-color 0.3s ease-in-out, transform 0.3s ease-in-out;
    }

    .view-students-btn:hover {
      background-color: #218838;
      color: white;
      transform: translateY(-3px);
      box-shadow: 0px 5px 15px rgba(255, 255, 255, 0.1);
    }

    @keyframes fadeIn {
      0% {
        opacity: 0;
        transform: translateY(10px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .btn-group {
      display: flex;
      justify-content: space-between;
      margin-top: 20px;
    }

    .btn {
      flex: 1;
      margin: 0 5px;
    }

  </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/"><i class="fas fa-user-graduate" style="color: white;"></i> Student Management</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item"><a class="nav-link" href="/"><i class="fas fa-home"></i> Home</a></li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="studentsDropdown" role="button">
                <i class="fas fa-user-graduate"></i> Students
              </a>
              <ul class="dropdown-menu" aria-labelledby="studentsDropdown">
                <li><a class="dropdown-item" href="/students"><i class="fas fa-user-plus"></i> Add Student</a></li>
                <li><a class="dropdown-item" href="/adminLogin"><i class="fas fa-users"></i> View Students</a></li>
              </ul>
            </li>
            <li class="nav-item"><a class="nav-link" href="/contact"><i class="fas fa-envelope"></i> Contact</a></li>
          </ul>
        </div>
      </div>
    </nav>
  
  <div class="container-fluid">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card p-4">
          <h2 class="text-center mb-4">Add New Student</h2>
          <form method="post" action="/submit">
            <div class="mb-3">
              <label for="idInput" class="form-label">
                <i class="fas fa-id-badge"></i> ID
              </label>
              <input type="number" class="form-control" id="idInput" placeholder="ID" name="id" autocomplete="off" required />
            </div>
            <div class="mb-3">
              <label for="nameInput" class="form-label">
                <i class="fas fa-user"></i> Name
              </label>
              <input type="text" class="form-control" id="nameInput" placeholder="Name"autocomplete="off" name="name" required />
            </div>
            <div class="mb-3">
              <label for="mobileInput" class="form-label">
                <i class="fas fa-phone"></i> Mobile
              </label>
              <input type="number" class="form-control" id="mobileInput"autocomplete="off" placeholder="Mobile" name="mobile" required />
            </div>
            <div class="mb-3">
              <label for="addressTextarea" class="form-label">
                <i class="fas fa-home"></i> Address
              </label>
              <textarea class="form-control" id="addressTextarea" rows="3"autocomplete="off" placeholder="Enter Address" name="address" required></textarea>
            </div>
            <div class="mb-3">
              <label for="cityDropdown" class="form-label">
                <i class="fas fa-city"></i> City
              </label>
              <select class="form-select" id="cityDropdown" autocomplete="off" name="city" required>
                <option value="Miraj">Miraj</option>
                <option value="Kolhapur">Kolhapur</option>
                <option value="Sangli">Sangli</option>
              </select>
            </div>
            <label for="hobbies" class="form-label">
              <i class="fas fa-star"></i> Hobbies
            </label><br />
            <div class="form-check">
              <input class="form-check-input" type="checkbox"autocomplete="off" value="Reading" id="hobbyReading" name="hobbie[]" />
              <label class="form-check-label" for="hobbyReading">Reading</label>
            </div>
            <div class="form-check">
              <input class="form-check-input" type="checkbox" autocomplete="off" value="Playing Tabala" id="hobbyTabala" name="hobbie[]" />
              <label class="form-check-label" for="hobbyTabala">Playing Tabala</label>
            </div>
            <div class="form-check">
              <input class="form-check-input" type="checkbox" autocomplete="off"  value="Swimming" id="hobbySwimming" name="hobbie[]" />
              <label class="form-check-label" for="hobbySwimming">Swimming</label>
            </div>
            <br>
            <label for="gender" class="form-label">
              <i class="fas fa-genderless"></i> Gender
            </label><br />
            <div class="form-check">
              <input class="form-check-input" type="radio" name="gender" id="genderMale" value="Male" required />
              <label class="form-check-label" for="genderMale">Male</label>
            </div>
            <div class="form-check">
              <input class="form-check-input" type="radio" name="gender" id="genderFemale" value="Female" required />
              <label class="form-check-label" for="genderFemale">Female</label>
            </div>

            <div class="btn-group">
              <button type="submit" class="btn btn-primary mt-3" name="submit">
                <i class="fas fa-user-plus"></i> Add Student
              </button>&nbsp;
              <a href="/adminLogin" class="btn view-students-btn mt-3">
                <i class="fas fa-users"></i> View Students
              </a>
            </div>
            
            
          </form>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
